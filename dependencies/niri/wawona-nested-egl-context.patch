diff --git a/src/backend/nested/mod.rs b/src/backend/nested/mod.rs
index 44d243b..c8425a4 100644
--- a/src/backend/nested/mod.rs
+++ b/src/backend/nested/mod.rs
@@ -454,19 +454,33 @@ impl Nested {
             debug: cfg!(debug_assertions),
             vsync: false,
         };
+        info!(
+            "nested: EGL display up; wayland_winsys={}",
+            egl_has_wayland_winsys()
+        );
+        // ANGLE on Apple/Android rejects 10-bit GLES 3.0
+        // (NoAvailablePixelFormat). Try 8-bit GLES 3.0 first.
         let context = EGLContext::new_with_config(
             &egl_display,
             gl_attributes,
-            PixelFormatRequirements::_10_bit(),
+            PixelFormatRequirements::_8_bit(),
         )
-        .or_else(|_| {
+        .or_else(|err| {
+            tracing::warn!("nested 8-bit GLES 3.0 context failed: {err:?}");
+            let gl2 = GlAttributes {
+                version: (2, 0),
+                profile: None,
+                debug: cfg!(debug_assertions),
+                vsync: false,
+            };
             EGLContext::new_with_config(
                 &egl_display,
-                gl_attributes,
+                gl2,
                 PixelFormatRequirements::_8_bit(),
             )
         })
-        .context("error creating the nested EGL context")?;
+        .map_err(|err| anyhow::anyhow!("error creating the nested EGL context: {err:?}"))?;
+        info!("nested: EGL context created");
 
         let present = if egl_has_wayland_winsys() {
             let wl_egl_surface =
diff --git a/src/c_api.rs b/src/c_api.rs
index 1fea551..913b1b1 100644
--- a/src/c_api.rs
+++ b/src/c_api.rs
@@ -39,6 +39,7 @@ pub extern "C" fn niri_main() -> c_int {
         Ok(Ok(())) => 0,
         Ok(Err(err)) => {
             eprintln!("niri_main: fatal error: {err:#}");
+            eprintln!("niri_main: fatal error (debug): {err:?}");
             1
         }
         Err(payload) => {
