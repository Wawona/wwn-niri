--- a/src/backend/nested/mod.rs
+++ b/src/backend/nested/mod.rs
@@ -84,29 +84,19 @@
 
 impl EGLNativeDisplay for NestedEglDisplay {
     fn supported_platforms(&self) -> Vec<EGLPlatform<'_>> {
-        let display = self.0;
-        // iOS Wawona advertises linux-dmabuf, so niri used to pick
-        // PLATFORM_WAYLAND_KHR, then Nested::new died before SHM present.
-        // WWN_NIRI_SHM / iOS: ANGLE Metal only, no Wayland native display.
-        // Do not require EGL_ANGLE_platform_angle* in the client string
-        // (iland/ANGLE on this guest does not advertise those names).
+        // iOS / WWN_NIRI_SHM: iland wraps WAYLAND and GBM only.
+        // PLATFORM_ANGLE_ANGLE is not a shim platform; eglInitialize then
+        // fails after "Successfully selected". GBM hands ANGLE DEFAULT.
         let force_shm = cfg!(target_os = "ios")
             || std::env::var_os("WWN_NIRI_SHM").is_some();
         if force_shm {
-            #[cfg(any(target_os = "ios", target_os = "tvos", target_os = "visionos", target_os = "macos"))]
-            {
-                return vec![egl_platform!(
-                    PLATFORM_ANGLE_ANGLE,
-                    std::ptr::null_mut(),
-                    &[],
-                    vec![
-                        ffi::egl::PLATFORM_ANGLE_TYPE_ANGLE,
-                        0x3489 as ffi::EGLint, // EGL_PLATFORM_ANGLE_TYPE_METAL_ANGLE
-                        ffi::egl::NONE as ffi::EGLint
-                    ]
-                )];
-            }
+            return vec![egl_platform!(
+                PLATFORM_GBM_KHR,
+                std::ptr::null_mut(),
+                &["EGL_KHR_platform_gbm"]
+            )];
         }
+        let display = self.0;
         vec![
             // see: https://www.khronos.org/registry/EGL/extensions/KHR/EGL_KHR_platform_wayland.txt
             egl_platform!(PLATFORM_WAYLAND_KHR, display, &["EGL_KHR_platform_wayland"]),
@@ -468,6 +458,9 @@
         // Apple targets this resolves to the wwn-iland/ANGLE EGL).
         let display_ptr = connection.backend().display_ptr() as *mut std::os::raw::c_void;
         let egl_display = unsafe { EGLDisplay::new(NestedEglDisplay(display_ptr)) }
+            .inspect_err(|err| {
+                eprintln!("niri: EGLDisplay::new failed: {err:?}");
+            })
             .context("error creating the nested EGL display")?;
 
         let gl_attributes = GlAttributes {
--- a/src/c_api.rs
+++ b/src/c_api.rs
@@ -73,6 +73,9 @@
         Ok(Err(err)) => {
             eprintln!("niri_main: fatal error: {err:#}");
             eprintln!("niri_main: fatal error (debug): {err:?}");
+            for (i, cause) in err.chain().enumerate() {
+                eprintln!("niri_main: cause[{i}]: {cause}");
+            }
             1
         }
         Err(payload) => {
@@ -131,7 +134,7 @@
         true,
         false,
     )
-    .map_err(|err| anyhow::anyhow!("error creating the compositor state: {err}"))?;
+    .map_err(|err| anyhow::Error::from(err).context("error creating the compositor state"))?;
 
     // Export the child socket for clients (fuzzel, etc.). niri itself already
     // holds an open connection to the host via the prior WAYLAND_DISPLAY value,
